const Match = require("../src/Match")

describe('Match', () => {

    let match
    beforeEach(() => {
        match = new Match()
    })

    it.todo("should be created with valid team names")
    it.todo("should throw an error when team names are invalid")
    it.todo("should have score 0-0 by default")
    it.todo("should update score")
    it.todo("should throw an error when updating with invalid score")
    it.todo("should calculate total score")

})