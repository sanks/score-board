const ScoreBoard = require("../src/ScoreBoard")

describe('ScoreBoard', () => {

    let sb
    beforeEach(() => {
        sb = new ScoreBoard()
    })

    it.todo("should start a multiple matches")
    it.todo("should finish match")
    it.todo("should throw an error if trying to finish non existing match")
    it.todo("should return list of matches ordered by total score desc")

})